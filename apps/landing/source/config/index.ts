import { Config } from '~/config/config.interfaces'

const config: Config = {
  project: {
    name: '@horus-software/landing',
    version: '0.0.0',
    description: 'horus.software landing page',
  },
}

export default config
